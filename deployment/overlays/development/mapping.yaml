apiVersion: getambassador.io/v2
kind: Mapping
metadata:
  name: treetracker-wallet-api-keycloak
spec:
  cors:
    origins: '*'
    methods: GET, POST, PATCH, DELETE, OPTIONS
    headers:
      - content-type
      - authorization
